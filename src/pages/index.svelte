<script>
  import { goto } from '@roxi/routify';
  // import { checkAuth } from '../stores/auth';

  // checkAuth().then(() => $goto('./templates')).catch(() => $goto('./oauth'))
  $goto('./templates')
</script>
